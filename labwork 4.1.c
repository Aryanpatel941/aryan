#include<stdio.h>
main()
{
	/*
	float x,y;
	printf(" enter the value of x :");
	scanf(" %f" , &x);
	printf(" enter the value of y :");
	scanf(" %f" , &y);
	float result=(x + y)*(x + y);
	printf(" the result of (x + y)*(x + y) is : %f\n\n\n ", result);
	*/
	
	/*
	float x,y;
	printf(" enter the value of x :");
	scanf(" %f" , &x);
	printf(" enter the value of y :");
	scanf(" %f" , &y);
	float  result=(x - y)*(x - y);
	printf(" the result of (x - y)*(x - y) is : %f ", result);
	*/
	
	/*
	float x,y;
	printf(" enter the value of x :");
	scanf(" %f" , &x);
	printf(" enter the value of y :");
	scanf(" %f" , &y);
	float result=(x + y)*(x + y)*(x + y);
	printf(" the result of (x + y)*(x + y)*(x + y) is : %f\n\n\n ", result);
	*/
	
	/*
	int a, b, c;
	printf("Enter value for a: ");
    scanf("%d", &a);  
	printf("Enter value for b: ");
    scanf("%d", &b); 
    c = a;  
    a = b;     
    b = c;  
    printf("Before swapping: a = %d, b = %d\n", a, b);
    printf("After swapping: a = %d, b = %d\n", a, b);
    */
    
    
    int a, b;
	printf("Enter value for a: ");
    scanf("%d", &a);  
	printf("Enter value for b: ");
    scanf("%d", &b); 
	printf("Before swapping: a = %d, b = %d\n", a, b);  
    a = a + b;
    b = a - b;
    a = a - b;
    printf("After swapping: a = %d, b = %d\n", a, b);
    


    
  


}

    
	
	


    

    



